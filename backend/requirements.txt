# AJIN BizAI Backend — requirements (최신화 2026-05-25)
#
# 실제 설치된 system Python 3.13 환경 기준 (pip freeze).
# Conda 환경(llm_api, bizai)에는 SQLAlchemy 누락 → system Python 권장.

# ─── 웹 프레임워크 ────────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.12
starlette==1.0.0

# ─── 환경변수 / 설정 ──────────────────────────────
python-dotenv==1.2.2
pydantic==2.13.3
pydantic-settings==2.14.0

# ─── 데이터베이스 ─────────────────────────────────
SQLAlchemy==2.0.49

# ─── HTTP 클라이언트 ──────────────────────────────
httpx==0.28.1                 # routers/notices.py, routers/files.py prefetch-url, services/lm_studio_client.py
requests==2.33.1              # 일부 외부 호출

# ─── 파일 파싱 ────────────────────────────────────
pdfplumber==0.11.9            # PDF 텍스트/표 추출
pypdfium2==5.7.1              # PDF → 이미지 렌더 (form_layout_builder)
pillow==12.2.0                # bbox overlay 이미지 생성
python-docx==1.1.2            # docx 입출력
python-pptx==1.0.2            # pptx export (선택)
openpyxl==3.1.5               # xlsx 처리
pandas==3.0.2                 # 데이터 가공
olefile==0.47                 # HWP 파일 읽기 (hwpx/hwp_reader.py) — 2026-05-25 신규

# ─── LLM 연동 ─────────────────────────────────────
openai==2.34.0                # services/openai_provider.py

# ─── Vector / Retrieval (E-2 evidence pipeline) ────
chromadb==1.5.8               # services/evidence_embedder.py / evidence_store.py
rank-bm25==0.2.2              # services/item_query_builder.py BM25

# ─── 테스트 ───────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0

# ─── 변경 이력 ─────────────────────────────────────
# 2026-05-25:
#   - olefile 신규 추가 (HWP 자료실 업로드 + DetailPage 첨부파일 본문 추출)
#   - 버전 갱신: fastapi 0.115→0.136, uvicorn 0.32→0.46, openai 1.14→2.34,
#                pandas 2.2→3.0, SQLAlchemy 2.0.28→2.0.49, httpx 0.27→0.28,
#                python-dotenv 1.0→1.2, pydantic-settings 2.2→2.14, pdfplumber 0.11.4→0.11.9
#   - 신규 명시: starlette, pydantic, requests, pypdfium2, pillow,
#               python-pptx, chromadb, rank-bm25, pytest, pytest-asyncio
#
# (참고) backend가 직접 import 하지 않는 transitive deps는 제외
#         streamlit/altair/plotly 등은 폐기된 /lab v0.1 잔재 (의도적 제외)
